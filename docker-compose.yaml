version: "3.8"

services:
  app:
    image: ghcr.io/ly921002/x-cf:reality
    container_name: x-cf
    restart: unless-stopped
    network_mode: host     # 使用宿主机网络
    environment:
      UUID: xxxx
      XRAY_PORT: 443
      BASE: api/v1
      PATH_LENGTH: 8  #随机路径长度

